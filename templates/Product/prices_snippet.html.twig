<table class="table product-prices-table">
    <thead>
    <tr>
        <th data-sort="int">#</th>
        <th data-sort="int">HQ</th>
        <th data-sort="int">Materia</th>
        <th data-sort="int">Price</th>
        <th data-sort="int">QTY</th>
        <th data-sort="int">Total</th>
        <th data-sort="string">Retainer</th>
        <th data-sort="string">Crafter</th>
        <th data-sort="int"></th>
    </tr>
    </thead>
    <tbody>
    {% for price in prices.Prices %}
        {% set state = (price.PriceTotal == stats.PriceTotal.Min ? 1 : (price.PriceTotal == stats.PriceTotal.Max ? 2 : 0)) %}
        <tr class="product-status-{{ state }}">
            <td align="center" class="product-num" width="1%" data-sort-value="{{ loop.index }}">{{ loop.index }}</td>
            <td align="center" class="product-hq" width="1%" data-sort-value="{{ price.IsHQ }}">{{ price.IsHQ ? '<img src="/i/hq.png">' : '' }}</td>
            <td align="center" class="product-materia" data-sort-value="{{ price.Materia|length }}">
                {% if price.Materia %}
                    {% for materia in price.Materia %}
                        <img src="{{ materia.Icon|icon }}">
                    {% endfor %}
                {% endif %}
            </td>
            <td align="right" class="product-ppu" data-sort-value="{{ price.PricePerUnit }}">
                {{ price.PricePerUnit|number_format }}
            </td>
            <td align="center" class="product-qty" width="1%" data-sort-value="{{ price.Quantity }}">
                {{ price.Quantity|number_format }}
            </td>
            <td align="right" class="product-pt" width="1%" data-sort-value="{{ price.PriceTotal }}">
                {{ price.PriceTotal|number_format }}
            </td>
            <td class="product-town" data-sort-value="{{ price.Town.Name }}">
                <img src="{{ price.Town.Icon|icon }}"> {{ price.Town.Name }}
            </td>
            <td class="product-crafter" data-sort-value="{{ price.CraftSignature }}">
                {% if price.CraftSignature|length > 2 %}
                    {{ price.CraftSignature }}
                {% endif %}
            </td>
            <td class="product-status" align="center" data-sort-value="">
                {% if state > 0 %}
                    <img src="/i/{{ state == 1 ? 'dot_green.png' : 'dot_red.png' }}">
                {% endif %}
            </td>
        </tr>
    {% else %}
        <tr>
            <td colspan="7">None for sale!</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
