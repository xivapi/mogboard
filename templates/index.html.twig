{% extends 'base.html.twig' %}

{% block body %}
    <main>

        <div class="home">

            <a href="{{ path('user_logout') }}" class="logout">Logout</a>
        </div>

    </main>
{% endblock %}
